# استبدال ملفات Life OS بعد التبسيط

هذه نسخة كاملة من الملفات، وليست مقتطفات أو patches. مبنية على آخر نسخة محلية تتضمن تحديث الأهداف والعادات والمال.

## الملفات السبعة المعدّلة

- core.js: توحيد توليد المعرّفات وإزالة النسخة المكررة داخل إكمال المهام؛ تنسيق مقروء.
- workspace.js: إزالة فرع المال القديم غير المستخدم، وإزالة وسيط إكمال المهام، وتوحيد التحقق من الميزانية في core.js، وحساب ملخص اليوم مرة واحدة؛ تنسيق مقروء.
- index.html: تحديث معرّف ملفات المتصفح لضمان تحميل النسخة الجديدة.
- sw.js: تحديث اسم ذاكرة الملفات وتنسيق الكود. لا تغيير في مفاتيح بيانات المستخدم.
- scripts/build.cjs: تنسيق مقروء فقط؛ يظل ناتج البناء dist.
- scripts/check.cjs: تنسيق مقروء فقط؛ نفس الفحوص.
- scripts/serve.cjs: تنسيق مقروء فقط؛ نفس التشغيل المحلي.

استبدل هذه الملفات في مساراتها نفسها مع الاحتفاظ ببقية المشروع. مجلد scripts يبقى بجانب package.json. لا تنقل الملفات التي بداخله إلى جذر المشروع.

إذا كانت نسختك المنشورة أقدم من تحديث المال والهوية، استخدم حزمة المشروع الكامل المرفقة لتجنب خلط إصدارات الملفات.

## البنية

core.js = البيانات والحسابات والمؤقت والتخزين.
workspace.js = واجهة مساحة العمل والتفاعل.
app.js = الدخول والصفحات الأصلية وجسر التوافق؛ ما زال مطلوبًا.
styles.css وexecutive.css = التنسيقات الحالية؛ ما زالا مطلوبين.
scripts = التشغيل والفحص والبناء.
tests = اختبارات السلوك.

لا مكتبات تشغيل جديدة، ولا تغيير framework أو routes أو schemaVersion أو localStorage keys.
لم تُحذف دوال اعتمادًا على البحث النصي وحده؛ بعض الارتباطات بين HTML والكود ديناميكية.

## التشغيل والتحقق

Node.js 24.x

npm ci
npm run lint
npm test
npm run build
npm start

العنوان المحلي الافتراضي: http://127.0.0.1:4173
Vercel: Build Command = npm run build، Output Directory = dist، ومجلد Root Directory هو الذي يحتوي package.json.

نجح البناء و24 اختبار سلوك. تمت مقارنة 42 حالة عرض بالعربية والإنجليزية مع النسخة السابقة وتطابقت. اختُبر رفض مجموع نسب 130% وحفظ النسب الصحيحة من الواجهة ببيانات QA منفصلة.

الاختبارات لا تمثل ضمانًا لكل جهاز ومتصفح. لا يوجد نشر أو دفع إلى GitHub، ولم تتغير ملفات النسخة العاملة الأصلية.
